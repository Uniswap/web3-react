branches:
  only:
  - latest
  - next
language: node_js
node_js:
- '10'
cache:
  directories:
  - node_modules
install: npm install
script:
  # - npm run lint
  - npm run build
  - npm run test
after_script:
  - npm run coverage | coveralls
